<h1>Admin Merchant Index</h1>

<% @merchants.each do |merchant| %>
<p><%=link_to merchant.name, "/admin/merchants/#{merchant.id}" %> </p>
<% end  %>



<% @merchants.each do |merchant| %>

          <%= merchant.id %>
          <%= link_to merchant.name, admin_merchants_path(merchant) %>
          <%= button_to "Disable", admin_merchants_path(merchant_id: merchant.id), method: :patch %>

    <% end %>

    <% @merchants.each do |merchant| %>

         <%= merchant.id %>
         <%= link_to merchant.name, admin_merchants_path(merchant) %>
         <%= button_to "Enable", admin_merchants_path(merchant_id: merchant.id), method: :patch %>

     <% end %>
